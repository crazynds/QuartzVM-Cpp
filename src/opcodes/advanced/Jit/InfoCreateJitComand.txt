INFORMAÇÕES NECESSÁRIAS PARA CRIAR UM COMANDO JIT:

Declaração:

case $COD_COMANDO:
	{if(t.checkUseCode(X))return 0;t.setPontCode(t.getPontCode()+X);}break; //Indique no X a quantidade de parametros que irá ter o comando.


Local das instruções:

case $COD_COMANDO:
case $COD_COMANDO2:{
	uintX val=t.getNextX(); // Colete todos os dados que são necessários para a execução do comando.
	
	
	a.mov(X,Y);
	a.mov(Y,VAL);	//Insira os comandos aqui do procedimento


	val_rax=LIXO_REV // Indique os registradores usados.
}break;
